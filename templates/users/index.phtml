<form action="/users">
    <input type="search" name ="user" value="<?= htmlspecialchars($userName) ?>">
    <input type="submit" value="Search">
</form>

<ul>
    <?php foreach ($users as $user): ?>
    <li><?= $user ?></li>
    <?php endforeach ?>
</ul>